// asddddddddddddddddddddddddd.cpp : Defines the entry point for the console application.
//

#include <iostream>
#include <set>
#include <vector>

using namespace std;

#define int long long

signed main()
{
	int a, b, n;
	cin >> a >> b >> n;
	vector<vector<int> > arr(n);
	for (int i = 0; i < n; ++i)
	{
		int l;
		cin >> l;
		for (int i = 0; i < l; ++i)
		{
			int x, y;
			cin >> x >> y;
			x -= a;

		}
	}
    return 0;
}

